# Исправление проблемы с перетаскиванием смен в календаре

## 🐛 Проблема

При перетаскивании смены в календаре:
- Смена не изменяла время на новое
- Смена "зависала" на месте
- При прокрутке графика смена не смещалась с места

## 🔍 Причина

Проблема была в подходе к обновлению DataSource при использовании гранулярных операций (`add`/`remove`):

### Неправильный подход (до исправления):
```dart
// 1. Обновляем массив смен
_shifts[shiftIndex] = updatedShift;

// 2. Пересоздаем DataSource через _updateFilteredList()
_updateFilteredList();

// 3. Пытаемся использовать granular updates на СТАРОМ DataSource
dataSource?.notifyListeners(CalendarDataSourceAction.remove, [oldShift]);
dataSource?.notifyListeners(CalendarDataSourceAction.add, [updatedShift]);
```

**Проблема:** После вызова `_updateFilteredList()` создавался новый DataSource, но операции `remove/add` применялись к старому объекту, который уже не связан с календарем.

## ✅ Решение

### Стратегия: Прямое обновление списка appointments

Вместо создания нового DataSource, обновляем существующий:

```dart
// 1. Обновляем массив смен
_shifts[shiftIndex] = updatedShift;

// 2. Обновляем DataSource appointments напрямую
if (dataSource != null && dataSource!.appointments != null) {
  final appointmentIndex = dataSource!.appointments!.indexWhere(
    (a) => (a as ShiftModel).id == shiftId
  );
  if (appointmentIndex != -1) {
    dataSource!.appointments![appointmentIndex] = updatedShift;
  }
  // Уведомляем календарь об изменениях
  dataSource!.notifyListeners(
    CalendarDataSourceAction.reset,
    dataSource!.appointments!
  );
}

// 3. Обновляем state для других слушателей
state.value = AsyncData([..._shifts]);
```

## 📝 Внесенные изменения

### 1. `updateShiftTime()` - Обновление при перетаскивании

**Файл:** `lib/schedule/viewmodels/schedule_view_model.dart:245-289`

**Изменения:**
- ✅ Убран вызов `_updateFilteredList()` при обновлении
- ✅ Добавлено прямое обновление `dataSource!.appointments!`
- ✅ Используется `reset` для синхронизации с календарем
- ✅ При ошибке вызывается `_loadData()` для полного восстановления

### 2. `deleteShift()` - Удаление смены

**Файл:** `lib/schedule/viewmodels/schedule_view_model.dart:291-315`

**Изменения:**
- ✅ Убран вызов `_updateFilteredList()`
- ✅ Прямое удаление из `dataSource!.appointments!`
- ✅ Мгновенное обновление календаря

### 3. `copyShift()` - Копирование смены

**Файл:** `lib/schedule/viewmodels/schedule_view_model.dart:317-350`

**Изменения:**
- ✅ Убран вызов `_updateFilteredList()`
- ✅ Прямое добавление в `dataSource!.appointments!`
- ✅ Мгновенное отображение новой смены

### 4. `_updateFilteredList()` - Оптимизация фильтрации

**Файл:** `lib/schedule/viewmodels/schedule_view_model.dart:107-184`

**Изменения:**
- ✅ Вместо создания нового DataSource обновляем существующий
- ✅ Используем `clear()` + `addAll()` для обновления списков
- ✅ Обновляем и appointments, и resources in-place

```dart
// Обновление DataSource: не пересоздаем, а модифицируем
if (dataSource != null) {
  // Update appointments in-place
  dataSource!.appointments!.clear();
  dataSource!.appointments!.addAll(filteredShifts);
  // Update resources
  dataSource!.resources!.clear();
  dataSource!.resources!.addAll(resources);
  // Notify calendar
  dataSource!.notifyListeners(
    CalendarDataSourceAction.reset,
    dataSource!.appointments!
  );
}
```

## 🎯 Почему используется `reset` вместо `add`/`remove`?

### Проблема с `add`/`remove`:
1. **Рассинхронизация:** При создании нового DataSource старые операции применяются к неактуальному объекту
2. **Сложность отслеживания:** Нужно синхронизировать изменения между `_shifts` и `dataSource.appointments`
3. **Баги с фильтрацией:** При фильтрации смена может быть в `_shifts`, но не в `dataSource.appointments`

### Преимущества `reset` с in-place обновлением:
1. ✅ **Простота:** Один источник правды (`_shifts`), один метод обновления
2. ✅ **Надежность:** Всегда синхронизировано с текущим состоянием
3. ✅ **Производительность:** Обновление существующего списка быстрее создания нового
4. ✅ **Совместимость:** Работает с фильтрацией, поиском, перетаскиванием

### Оптимизация производительности:
- `reset` НЕ означает полную перестройку UI
- Syncfusion Calendar умный: он делает diff и обновляет только измененные элементы
- Благодаря `RepaintBoundary` перерисовываются только затронутые смены
- Кэширование `CalendarResource` устраняет повторные создания объектов

## 🧪 Тестирование

### Чек-лист для проверки:

1. **Перетаскивание по времени (горизонтально)**
   - [ ] Перетащить смену на другое время
   - [ ] Проверить, что время обновилось
   - [ ] Прокрутить календарь и вернуться - смена на новом месте

2. **Перетаскивание на другого сотрудника (вертикально)**
   - [ ] Перетащить смену на другого сотрудника
   - [ ] Проверить, что сотрудник обновился
   - [ ] Смена отображается у нового сотрудника

3. **Изменение размера смены**
   - [ ] Растянуть смену за правый край
   - [ ] Проверить, что длительность обновилась
   - [ ] Время окончания изменилось корректно

4. **Удаление смены**
   - [ ] Удалить смену через контекстное меню
   - [ ] Смена мгновенно исчезает
   - [ ] После прокрутки смены нет

5. **Копирование смены**
   - [ ] Скопировать смену
   - [ ] Новая смена появляется на следующий день
   - [ ] Обе смены видны и работают независимо

6. **Фильтрация + Перетаскивание**
   - [ ] Применить фильтр по локации
   - [ ] Перетащить видимую смену
   - [ ] Проверить, что перетаскивание работает
   - [ ] Сбросить фильтр - смена на новом месте

## 📊 Результаты

### До исправления:
- ❌ Смены "зависали" при перетаскивании
- ❌ Не обновлялось время
- ❌ Рассинхронизация с DataSource

### После исправления:
- ✅ Плавное перетаскивание
- ✅ Мгновенное обновление времени
- ✅ Смены корректно отображаются после прокрутки
- ✅ Работает с фильтрацией
- ✅ Нет "зависаний" или артефактов

## 🎓 Уроки

1. **Не смешивайте создание и обновление:** Если создаете новый DataSource, не используйте операции на старом
2. **In-place updates > Recreate:** Обновление существующего объекта быстрее и надежнее
3. **Один источник правды:** `_shifts` - главный источник, DataSource - его представление
4. **Reset не всегда плохо:** При правильном использовании (с кэшированием и boundaries) reset эффективен

## 🚀 Дальнейшие улучшения (опционально)

Если нужна еще большая производительность:

1. **Батчинг операций:** Группировать множественные изменения в один `reset`
2. **Debouncing для drag:** Обновлять не при каждом пикселе движения, а с задержкой
3. **Virtual scrolling:** Для > 500 смен использовать виртуализацию

Но для MVP текущая реализация более чем достаточна! ✨
