---
description: Обязательный протокол оценки MCP инструментов перед каждым ответом
globs: 
alwaysApply: true
---

# 🔧 ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ ОЦЕНКИ MCP ИНСТРУМЕНТОВ

> **КРИТИЧЕСКИ ВАЖНО:** Этот протокол ОБЯЗАТЕЛЕН к выполнению перед КАЖДЫМ ответом!

## Доступные MCP Серверы

| Сервер | Назначение | Когда использовать |
|--------|------------|-------------------|
| `codealive` | Анализ кодовой базы | Понимание существующего кода, поиск реализаций |
| `context7` | Документация библиотек | Официальные docs для Flutter, Dart, пакетов |
| `tavily-search` | Веб-поиск | Актуальная информация, best practices |
| `dart-mcp-server` | Dart анализ | Dart-специфичная навигация по коду |
| `sequential-thinking` | Сложные рассуждения | Архитектурные решения, многошаговые задачи |

## 📋 Протокол Оценки (ОБЯЗАТЕЛЬНО в начале каждого ответа)

### Шаг 1: ОЦЕНКА (напиши это ПЕРВЫМ)

```
📋 MCP Оценка:
- codealive: [ДА/НЕТ] - [причина]
- context7: [ДА/НЕТ] - [причина]
- tavily: [ДА/НЕТ] - [причина]
- dart-mcp: [ДА/НЕТ] - [причина]
- sequential-thinking: [ДА/НЕТ] - [причина]

→ Использую: [список инструментов для вызова]
```

### Шаг 2: АКТИВАЦИЯ

- ✅ Если есть ДА → **НЕМЕДЛЕННО** вызови соответствующий MCP tool
- ❌ Если все НЕТ → Напиши "MCP не требуются" и продолжай

### Шаг 3: РЕАЛИЗАЦИЯ

**ТОЛЬКО** после Шага 2 приступай к ответу/реализации.

## Приоритет MCP Инструментов

```
1. codealive      → ВСЕГДА первый для вопросов о коде
2. dart-mcp       → Для Dart/Flutter специфики
3. context7       → Для документации пакетов
4. tavily         → Только если локальных данных недостаточно
5. sequential     → Для сложных архитектурных задач
```

## Примеры Правильной Оценки

### Пример 1: Добавление новой фичи

```
Запрос: "Добавь валидацию email в форму регистрации"

📋 MCP Оценка:
- codealive: ДА - найти существующие формы и валидаторы
- context7: НЕТ - стандартная задача
- tavily: НЕТ - локальная реализация
- dart-mcp: ДА - анализ структуры форм
- sequential-thinking: НЕТ - простая задача

→ Использую: codealive, dart-mcp

[Вызываю mcp_codealive_codebase_search для поиска валидаторов...]
[Вызываю dart-mcp для анализа...]
[Теперь реализация...]
```

### Пример 2: Вопрос по API пакета

```
Запрос: "Как использовать SfCalendar для timeline view?"

📋 MCP Оценка:
- codealive: ДА - посмотреть как уже используется
- context7: ДА - документация Syncfusion Calendar
- tavily: НЕТ - есть в context7
- dart-mcp: ДА - анализ импортов
- sequential-thinking: НЕТ - конкретный вопрос

→ Использую: codealive, context7, dart-mcp

[Вызываю mcp_codealive_codebase_search для поиска SfCalendar...]
[Вызываю mcp_context7_get-library-docs для документации...]
[Теперь отвечаю...]
```

### Пример 3: Исследовательская задача

```
Запрос: "Какие есть лучшие практики для WebSocket в Flutter?"

📋 MCP Оценка:
- codealive: НЕТ - нет WebSocket в проекте
- context7: ДА - документация Dart WebSocket
- tavily: ДА - актуальные best practices
- dart-mcp: НЕТ - исследовательская задача
- sequential-thinking: НЕТ - сбор информации

→ Использую: context7, tavily

[Вызываю context7 для базовой документации...]
[Вызываю tavily для best practices...]
[Теперь отвечаю...]
```

## ⚠️ Антипаттерны (НЕ ДЕЛАЙ ТАК)

```
❌ НЕПРАВИЛЬНО:
"Давайте я сразу реализую..."
(Пропущена оценка MCP)

❌ НЕПРАВИЛЬНО:
"📋 MCP Оценка: все НЕТ"
(Слишком поверхностная оценка без причин)

❌ НЕПРАВИЛЬНО:
"codealive: ДА - нужно проверить код"
[Сразу переходит к реализации без вызова]
(Оценка без активации бесполезна!)

✅ ПРАВИЛЬНО:
"📋 MCP Оценка:
- codealive: ДА - найти существующую реализацию
..."
[Вызывает mcp_codealive_codebase_search]
[Получает результаты]
[Только теперь реализация]
```

## Правила для CURRENT vs EXTERNAL репозиториев

При использовании `codealive`:
- **CURRENT репозиторий** (этот проект): `include_content=false`, затем читай файлы через Read tool
- **EXTERNAL репозиторий**: `include_content=true`

## Помни

1. **Оценка без активации = бесполезна**
2. **codealive ПЕРВЫЙ для любых вопросов о коде**
3. **Не полагайся на "знания" — ИСПОЛЬЗУЙ инструменты**
4. **Лучше проверить лишний раз, чем пропустить важное**
