---
alwaysApply: true
---

ТЗ:

Задачи для верстки :
1. Создать пользовательский интерфейс для управления сотрудниками с
возможностью добавления, редактирования и удаления.
2. Реализовать календарь для визуализации и корректировки смен.
3. Сделать формы для планирования смен с учётом пожеланий сотрудников.
4. Добавить интерфейс для просмотра и управления уведомлениями.
5. Обеспечить взаимодействие с сервером через REST API (отправка и
получение данных в формате JSON).
6. Сделать интерфейс отзывчивым (адаптивным) и удобным для
пользователей.
Задачи для логики :
1. Реализовать API для работы с базой данных сотрудников, смен и
расписаний.
2. Организовать хранение и обработку данных в реляционной базе
(например, PostgreSQL).
3. Написать логику для создания, редактирования и удаления графиков смен.
4. Учесть учёт рабочего времени и историю трудовой деятельности каждого
сотрудника.
5. Создать модуль уведомлений для оповещения сотрудников об изменениях
графика работы.
6. Обеспечить аутентификацию и авторизацию пользователей.
7. Организовать стабильное и безопасное взаимодействие между клиентской
и серверной частями.
ТЗ
Тема: Создание программного обеспечения для автоматизации процессов
управления сменами персонала и оперативного информирования
сотрудников.
Модуль управления персоналом:
· Ведение базы данных сотрудников (ФИО, должность, филиал)
· Учет рабочего времени и смен
· История трудовой деятельности
1.2. Модуль планирования смен:
· Формирование графиков работы
· Учет пожеланий сотрудников 
1.3. Коммуникационный модуль:
· Система уведомлений об изменениях графика
· Рассылка объявлений
1.3. Архитектура системы:
· Клиент-серверная архитектура
· Веб-интерфейс
· Реляционная база данных
2. Клиент (веб-интерфейс)
• Это интерфейс пользователя, доступный через браузер.
• Клиент отвечает за отображение данных и взаимодействие с
пользователем: формы для ввода информации о сотрудниках, календарь
смен, просмотр уведомлений.
• Все действия пользователя приводят к отправке запросов на сервер,
например, получить список смен или создать новый график.
3. Сервер
• Сервер принимает запросы от клиента, обрабатывает их и отвечает
результатами.
• Он содержит бизнес-логику: управление базой данных сотрудников,
формирование и изменение графиков работы, учёт пожеланий
сотрудников.
• Также сервер реализует коммуникационный модуль — отправку
уведомлений о изменениях и рассылку объявлений.
4. Реляционная база данных
• Центральное место хранения всей информации: данные сотрудников
(ФИО, должность, филиал), смены, графики, история трудовой
деятельности.
• Данные организованы в таблицах с отношениями (например, один
сотрудник может иметь несколько смен).
• База обеспечивает целостность, быстрое хранение и поиск данных.



Из тз нужно сделать только frontend MVP.




***

# 🗂️ plan.mdc — План разработки Shift Manager (MVP)

> **⚠️ КОНТЕКСТ:**
> *   **Архитектура:** Feature-First MVVM (Hungrimind Modified)
> *   **Data Layer:** Repository Pattern + Mock Services (имитация REST API)
> *   **UI:** Material 3 + Специализированные пакеты (PlutoGrid, Syncfusion, TimelineTile)
> *   **Backend:** Отсутствует (все данные генерируются локально в `MockApiService`)

***

## 🧱 ЭТАП 1: Фундамент (Архитектура и База)
**Цель:** Подготовить "скелет", на который мы будем наращивать "мясо" (экраны). Без этого этапа код превратится в спагетти.

### ✅ День 1: Инициализация и Структура (ЗАВЕРШЕН 2025-11-28)
*   [x] **Project Setup:** Создать новый Flutter проект.
*   [x] **Rules:** Создать папку `.cursor/rules` и добавить файлы `architecture.mdc` и `conventions.mdc` (с нашими правками про Mock/Repo).
*   [x] **Folders:** Создать структуру папок по шаблону (lib/data, lib/features, lib/config).
*   [x] **Routing:** Настроить существующую систему навигации в `lib/config/route_config.dart` (пока пустые заглушки экранов).

### ✅ День 2: Слой Данных (The "Fake" Backend) (ЗАВЕРШЕН 2025-11-28)
*   [x] **Models:** Создать DTO в `lib/data/models/` (User, Employee, Shift).
    *   *Важно:* Использовать `String id` (UUID) и `snake_case` в `fromJson`.
*   [x] **API Service:** Создать `lib/data/services/api_service.dart` (Abstract Interface).
*   [x] **Mock Implementation:** Реализовать `lib/data/services/mock_api_service.dart`.
    *   *Задача:* Написать методы генерации 50 сотрудников и 20 смен с задержкой 800мс.
*   [x] **Locator:** Зарегистрировать MockService в `get_it`.

### ✅ День 2.5: Критические исправления и улучшения (ЗАВЕРШЕН 2025-11-28)
*   [x] **Локализация:** Удалить `generate: true` из pubspec.yaml
*   [x] **AuthService:** Создать `lib/core/services/auth_service.dart` для session management
*   [x] **Route Guards:** Добавить `requiresAuth` флаг и проверку авторизации в RouterDelegate
*   [x] **Syncfusion License:** Активировать 7-дневную пробную лицензию
*   [x] **AsyncValue:** Создать `lib/core/utils/async_value.dart` для обработки состояний
*   [x] **State Management Guide:** Создать детальный гайд с примерами
*   [x] **Memory Leaks Prevention:** Создать гайд по предотвращению утечек памяти
*   [x] **Error Handling Strategy:** Создать единую стратегию обработки ошибок
*   [x] **Mock Data Persistence:** Разработать стратегию персистентности для MVP
*   [x] **UUID Generation:** Создать гайд по генерации уникальных ID
*   [x] **Architecture Rules:** Обновить правила, устранить противоречия, добавить диаграмму зависимостей

### 🔄 День 3: Авторизация (Входная точка) (ТЕКУЩИЙ ЭТАП)

> **📚 Полное руководство:** `docs/login_screen_implementation_guide.md`

#### Model (`lib/data/models/user_model.dart`)
*   [x] **User модель** уже создана ✅ ГОТОВО

#### ViewModel (`lib/auth/viewmodels/auth_view_model.dart`)
*   [ ] **Создать AuthViewModel** (БЕЗ ChangeNotifier)
*   [ ] **Добавить поля:**
    *   `final AuthRepository _authRepository` - репозиторий для авторизации
    *   `final loginState = ValueNotifier<AsyncValue<void>>()` - состояние логина
*   [ ] **Реализовать методы:**
    *   `Future<void> login(String email, String password)` - авторизация через Repository
    *   `void dispose()` - очистка ресурсов
*   [ ] **Mock credentials (в MockApiService):**
    *   Email: `admin@example.com`
    *   Password: `password123`
    *   Задержка: 1 секунда

#### View (`lib/auth/views/login_view.dart`)
*   [ ] **Создать LoginView** (StatefulWidget)
*   [ ] **Форма с валидацией:**
    *   Email поле (prefixIcon: mail, валидация: не пустой + содержит '@')
    *   Password поле (prefixIcon: lock, suffixIcon: показать/скрыть, мин 6 символов)
*   [ ] **UI элементы:**
    *   Фон: #F5F7FA (светло-серый)
    *   Карточка: белая, тень, скругление 16px, maxWidth 400px
    *   Кнопка "Войти": #007AFF, высота 48px
    *   Loading state: CircularProgressIndicator вместо текста
    *   Error message: красный контейнер с иконкой
    *   Подсказка с тестовыми данными
*   [ ] **Стилизация:**
    *   Google Fonts (Inter или Roboto)
    *   Material 3
    *   Адаптивный дизайн

#### Интеграция
*   [ ] **ViewModel setup:**
    *   Инициализировать `AuthViewModel` в `initState()`
    *   Использовать `ValueListenableBuilder` для отслеживания состояния
    *   Вызвать `dispose()` в `dispose()`
*   [ ] **Навигация:**
    *   При успехе -> редирект на `/dashboard` через `RouterService`
    *   Использовать `locator<RouterService>().pushReplacementNamed()`
*   [ ] **Обработка ошибок:**
    *   Показ ошибки через `AsyncError` в `AsyncValue`
    *   Автоматическая очистка при новой попытке

***

## 👥 ЭТАП 2: Управление Сотрудниками (Tabular Data)
**Цель:** Реализовать работу с табличными данными, используя PlutoGrid или Syncfusion DataGrid.

### ✅ День 4: Dashboard Shell
*   [ ] **Layout:** Создать `DashboardView` с боковым меню (`NavigationRail` для Desktop / `Drawer` для Mobile).
*   [ ] **Navigation:** Настроить переключение между экранами через RouterService.

### 📊 День 5: Список Сотрудников - Выбор решения

> **⚠️ ВАЖНО:** Реализовать обе версии для сравнения, затем выбрать лучшую.

#### Вариант A: PlutoGrid (Рекомендуется для MVP) 🏆
*   [ ] **Model:** `EmployeeListModel` с enum `EmployeeStatus` (onShift, dayOff, vacation)
*   [ ] **ViewModel:** `EmployeeListViewModel` (ChangeNotifier) с методом `getPlutoRows()`
*   [ ] **UI:** Реализация `EmployeeListView` с PlutoGrid
    *   *Колонки:* Name (с аватаром), Role, Branch, Status (цветные бейджи), Hours, Actions
    *   *Рендереры:* Кастомный рендер для Name (CircleAvatar + Text), Status (цветные Chip)
    *   *Фильтрация:* Branch и Status через PlutoColumnType.select()
    *   *Поиск:* По имени через TextField в header
*   [ ] **Pagination:** PlutoPagination в createFooter (20 строк на страницу)
*   [ ] **Styling:** Clean SaaS стиль (rowHeight: 60, белый фон, минимальные границы)
*   [ ] **Mock Data:** 50 сотрудников с аватарами (https://i.pravatar.cc/150?u=$id)
*   **См. детали:** `docs/employee_list_implementation_guide.md`

**Преимущества PlutoGrid:**
- ✅ Лицензия MIT (без ограничений)
- ✅ Меньший размер bundle (+1.2MB vs +4.5MB)
- ✅ Проще в использовании (1-2 часа обучения)
- ✅ Встроенная фильтрация
- ✅ Достаточная производительность для 50-500 сотрудников

#### Вариант B: Syncfusion DataGrid (Для сравнения)
*   [ ] **Model:** `EmployeeSyncfusionModel` с enum `EmployeeStatus`
*   [ ] **DataSource:** `EmployeeDataSource` (наследник `DataGridSource`)
*   [ ] **ViewModel:** `EmployeeSyncfusionViewModel` (ChangeNotifier)
*   [ ] **UI:** Реализация `EmployeeSyncfusionView` с `SfDataGrid`
    *   *Колонки:* Те же, что в PlutoGrid
    *   *Cell Builders:* Кастомные виджеты через `buildRow`
    *   *Пагинация:* `SfDataPager` (20 строк на страницу)
    *   *Сортировка:* Через `handleSort` в DataSource
*   **См. детали:** `docs/employee_list_syncfusion_guide.md`

**Преимущества Syncfusion:**
- ✅ Лучшая производительность на больших данных (5000+ строк)
- ✅ Встроенный экспорт в Excel/PDF
- ✅ Отличная документация
- ⚠️ Требует лицензию (бесплатно до $1M годового дохода)

#### Сравнение решений

**См. полное сравнение:** `docs/table_comparison_pluto_vs_syncfusion.md`

| Критерий | PlutoGrid | Syncfusion | Победитель |
|----------|-----------|------------|------------|
| **Лицензирование** | MIT (Open Source) | Community License | 🏆 PlutoGrid |
| **Производительность** | 8/10 | 10/10 | 🏆 Syncfusion |
| **Функциональность** | 9/10 | 9/10 | 🤝 Равны |
| **Размер bundle** | +1.2MB | +4.5MB | 🏆 PlutoGrid |
| **Простота** | 9/10 | 7/10 | 🏆 PlutoGrid |
| **Документация** | 7/10 | 10/10 | 🏆 Syncfusion |

**Итоговая оценка:**
- **PlutoGrid**: 8.75/10 🏆
- **Syncfusion**: 8.35/10

**Рекомендация:** Начать с PlutoGrid для MVP, при необходимости мигрировать на Syncfusion (время миграции: 4-6 часов).

### ✅ День 6: Фильтрация и Поиск
*   [ ] **Logic:** Добавить методы фильтрации в ViewModel (поиск по имени, фильтр по филиалу).
*   [ ] **UI:** Верстка хедера над таблицей (Search Bar + Dropdowns).
*   [ ] **Интеграция:** Связать Dropdown с фильтрами PlutoGrid.

### ✅ День 7: Профиль Сотрудника (Visuals)

> **📚 Полное руководство:** `docs/profile_screen_implementation_guide.md`

#### Model (`lib/employees/models/profile_model.dart`)
*   [ ] **Создать HistoryEvent** с полями:
    *   `DateTime date` - дата события
    *   `String title` - заголовок
    *   `String description` - описание
*   [ ] **Создать EmployeeProfile** с полями:
    *   `id`, `name`, `role`, `avatarUrl`
    *   `email`, `phone`, `address`
    *   `branch`, `hireDate`
    *   `List<HistoryEvent> history` - история событий
    *   `double workedHours`, `double totalHours` - для прогресса
*   [ ] **Добавить вычисляемое свойство** `hoursPercent`
*   [ ] **Реализовать factory методы** для моковых данных

#### ViewModel (`lib/employees/viewmodels/profile_view_model.dart`)
*   [ ] **Создать ProfileViewModel** (БЕЗ ChangeNotifier)
*   [ ] **Добавить поля:**
    *   `final profileState = ValueNotifier<AsyncValue<EmployeeProfile>>()` - состояние профиля
*   [ ] **Реализовать методы:**
    *   `Future<void> loadProfile(String id)` - загрузка с задержкой 1 сек
    *   `void clearProfile()` - очистка данных
    *   `void dispose()` - очистка ресурсов
*   [ ] **Mock данные:**
    *   3-4 события истории
    *   Заполненность часов 80% (128 из 160)

#### View (`lib/employees/views/profile_view.dart`)
*   [ ] **Создать ProfileView** (StatefulWidget)
*   [ ] **Адаптивный Layout** через `LayoutBuilder`:
    *   Desktop (>900px): Grid с 2 колонками
    *   Mobile (≤900px): Column (одна под другой)

#### Карточки (Cards)

**A. Header Card:**
*   [ ] Аватар (CircleAvatar, radius 40)
*   [ ] Имя и роль
*   [ ] Бейдж с филиалом
*   [ ] Кнопка "Редактировать"

**B. Info Card (Контактная информация):**
*   [ ] Email с иконкой
*   [ ] Телефон с иконкой
*   [ ] Адрес с иконкой
*   [ ] Дата приема с иконкой

**C. Time Card (Учет времени) с `percent_indicator`:**
*   [ ] Заголовок "Учет времени"
*   [ ] Текст "Отработано: X из Y ч"
*   [ ] `LinearPercentIndicator`:
    *   `lineHeight: 12.0`
    *   `percent: workedHours / totalHours`
    *   `backgroundColor: Colors.grey.shade200`
    *   `progressColor: Colors.blue`
    *   `barRadius: Radius.circular(6)`
    *   `animation: true`
    *   `center: Text("X%")` - белый текст

**D. History Card (История) с `timeline_tile`:**
*   [ ] Заголовок "История"
*   [ ] `ListView.builder` с `TimelineTile`:
    *   `alignment: TimelineAlign.start`
    *   `isFirst` / `isLast` - корректная обработка
    *   `IndicatorStyle`:
      - Первое событие: синий круг
      - Остальные: серый круг
    *   `LineStyle`: серая линия, толщина 2
    *   `endChild`: Контейнер с:
      - Дата (серый, 12px)
      - Заголовок (жирный, 16px)
      - Описание (серый, 14px)

#### Стилизация
*   [ ] Все карточки: белые, elevation 2, borderRadius 12px
*   [ ] Отступы: padding 24px внутри карточек
*   [ ] Шрифты: стандартные Material
*   [ ] Цвета: синий для акцентов, серый для второстепенного

***

## 📅 ЭТАП 3: График Смен (Complex Logic)
**Цель:** Самая сложная часть системы. Визуализация времени и ресурсов с использованием Syncfusion Calendar.

> **📚 Полное руководство:** `docs/schedule_screen_implementation_guide.md`

### ✅ День 8: Подготовка Данных Календаря

#### Model (`lib/schedule/models/shift_model.dart`)
*   [ ] **Создать ShiftModel** с полями:
    *   `String id` - уникальный идентификатор
    *   `String employeeId` - ID сотрудника
    *   `DateTime startTime` - начало смены
    *   `DateTime endTime` - конец смены
    *   `String roleTitle` - должность (Администратор, Повар и т.д.)
    *   `String location` - локация (ТЦ Мега, Центр, Аэропорт)
    *   `Color color` - цвет смены
*   [ ] **Добавить вычисляемые свойства:**
    *   `durationInHours` - длительность в часах
    *   `timeRange` - форматированное время (09:00 - 18:00)
*   [ ] **Реализовать factory метод** `ShiftModel.mock()` для генерации тестовых данных
*   [ ] **Добавить метод** `copyWith()` для иммутабельных обновлений

#### DataSource (`lib/schedule/viewmodels/shift_data_source.dart`)
*   [ ] **Создать ShiftDataSource** (наследник `CalendarDataSource`)
*   [ ] **Переопределить методы:**
    *   `getStartTime()` - возвращает startTime смены
    *   `getEndTime()` - возвращает endTime смены
    *   `getSubject()` - возвращает roleTitle
    *   `getColor()` - возвращает color смены
    *   `getResourceIds()` - возвращает [employeeId]
    *   `getNotes()` - возвращает location
    *   `getId()` - возвращает id смены

#### Mock Data
*   [ ] **Сгенерировать 10 смен** разной длины:
    *   Дневные смены (09:00-18:00, 12:00-20:00)
    *   Вечерние смены (18:00-00:00)
    *   Ночные смены (00:00-08:00)
*   [ ] **Распределить смены** между 5 сотрудниками
*   [ ] **Создать сложные сценарии:**
    *   Пересечения смен (для проверки конфликтов)
    *   Смены на разных локациях
    *   Разные роли для одного сотрудника

### ✅ День 9: ViewModel и Бизнес-логика

#### ViewModel (`lib/schedule/viewmodels/schedule_view_model.dart`)
*   [ ] **Создать ScheduleViewModel** (extends ChangeNotifier)
*   [ ] **Добавить поля:**
    *   `List<ShiftModel> _shifts` - список смен
    *   `List<CalendarResource> _resources` - список сотрудников
    *   `ShiftDataSource _dataSource` - источник данных для календаря
*   [ ] **Реализовать методы:**
    *   `_loadMockData()` - загрузка моковых данных
    *   `_generateMockEmployees()` - генерация 5 сотрудников
    *   `_generateMockShifts()` - генерация смен на неделю
    *   `addShift(ShiftModel)` - добавление смены
    *   `deleteShift(String id)` - удаление смены
    *   `updateShift(ShiftModel)` - обновление смены
    *   `getShiftsForEmployee(String id)` - получение смен сотрудника
    *   `hasConflict(ShiftModel)` - проверка конфликтов
*   [ ] **Преобразовать сотрудников** в `CalendarResource`:
    *   `id` - ID сотрудника
    *   `displayName` - имя сотрудника
    *   `color` - цвет для визуализации
    *   `image` - NetworkImage с аватаром

### ✅ День 10: Визуализация (Syncfusion Timeline)

#### View (`lib/schedule/views/schedule_view.dart`)
*   [ ] **Создать ScheduleView** (StatefulWidget)
*   [ ] **Настроить SfCalendar:**
    *   `view: CalendarView.timelineWeek` - режим Timeline
    *   `dataSource: viewModel.dataSource` - источник данных
    *   `firstDayOfWeek: 1` - начало недели с понедельника
    *   `showCurrentTimeIndicator: true` - показать текущее время

#### TimeSlotViewSettings (Настройки временных слотов)
*   [ ] **Настроить параметры:**
    *   `timelineAppointmentHeight: 60` - высота смены
    *   `timeInterval: Duration(hours: 4)` - интервал 4 часа
    *   `dateFormat: 'd'` - формат даты
    *   `dayFormat: 'EEE'` - формат дня недели
    *   `startHour: 0, endHour: 24` - показать весь день

#### ResourceViewSettings (Настройки ресурсов)
*   [ ] **Настроить параметры:**
    *   `visibleResourceCount: 5` - показать 5 сотрудников
    *   `showAvatar: true` - показать аватары
    *   `size: 150` - ширина колонки сотрудников
    *   `displayNameTextStyle` - стиль имени (14px, bold)

#### Custom Appointment Builder (Кастомные карточки смен)
*   [ ] **Реализовать appointmentBuilder:**
    *   `Container` с закруглением (borderRadius: 8)
    *   Левый бордер 4px (цвет статуса)
    *   Фон: `color.withOpacity(0.1)`
*   [ ] **Содержимое карточки:**
    *   **Верхняя строка:** Время (09:00 - 18:00) - жирный шрифт, цвет смены
    *   **Средняя строка:** Роль (Администратор) - 11px, bold
    *   **Нижняя строка:** Локация (ТЦ Мега) - 10px, серый

#### Стилизация
*   [ ] **HeaderStyle:** Заголовок календаря (18px, bold)
*   [ ] **ViewHeaderStyle:** Дни недели (14px, bold) + даты (20px, bold)
*   [ ] **Цвета:** Белый фон, серые границы, синяя линия текущего времени

### ✅ День 11: Интерактивность и Диалоги

#### Обработка событий
*   [ ] **onTap:** Обработка тапа по смене
    *   Получить `ShiftModel` из `details.appointments`
    *   Показать диалог с деталями смены
*   [ ] **Диалог деталей смены:**
    *   Время смены
    *   Локация
    *   Длительность в часах
    *   Кнопка "Удалить" (красная)
    *   Кнопка "Закрыть"

#### AppBar Actions
*   [ ] **Кнопка "Добавить смену"** (IconButton с Icons.add)
    *   Открывает диалог создания смены
*   [ ] **Кнопка "Фильтры"** (IconButton с Icons.filter_list)
    *   Фильтрация по сотруднику, локации, роли

### ✅ День 12: Создание и Редактирование Смен

> **📚 Полное руководство:** `docs/create_shift_dialog_implementation_guide.md`

#### Create Shift Dialog (`lib/schedule/widgets/create_shift_dialog.dart`)
*   [ ] **Базовый UI:**
    *   `Dialog` с `ConstrainedBox(maxWidth: 500)`
    *   Заголовок "Создать смену"
    *   Кнопка закрытия (X)
*   [ ] **Форма с полями:**
    *   Dropdown "Сотрудник" (3 варианта)
    *   Dropdown "Должность" (4 варианта)
    *   DatePicker "Дата"
    *   TimePicker "Начало"
    *   TimePicker "Конец"
    *   Text "Длительность: X ч" (автоматический расчет)
    *   Dropdown "Филиал" (3 варианта)
*   [ ] **Валидация:**
    *   Все поля обязательны
    *   Конец не может быть раньше начала
    *   Минимальная длительность 2 часа
*   [ ] **Логика конфликтов (Critical):**
    *   Hardcoded: Если выбран "Иван Иванов" (id=1)
    *   Показать оранжевый Warning Box:
      - Иконка warning
      - Текст "⚠️ Сотрудник просил выходной"
      - Checkbox "Игнорировать предупреждение"
    *   Если checkbox не нажат -> кнопка "Сохранить" disabled
*   [ ] **Логика сохранения:**
    *   Показать SnackBar: "Смена создана. Уведомление отправлено"
    *   Вернуть `true` через `Navigator.pop(context, true)`

#### Интеграция в Schedule Screen
*   [ ] **Добавить кнопку в AppBar:**
    *   `ElevatedButton.icon`
    *   Icon: `Icons.add`
    *   Label: "Добавить смену"
    *   Color: Blue
    *   Padding: horizontal 16, vertical 8
*   [ ] **Реализовать метод `_showCreateShiftDialog()`:**
    ```dart
    final result = await showDialog<bool>(
      context: context,
      builder: (ctx) => const CreateShiftDialog(),
    );
    if (result == true && mounted) {
      // Обновить график
      viewModel.refreshShifts();
    }
    ```
*   [ ] **Добавить импорт:**
    ```dart
    import 'package:my_app/schedule/widgets/create_shift_dialog.dart';
    ```

#### Flow создания смены:
1. Тап на "Добавить смену" в AppBar
2. Открытие модального окна
3. Заполнение формы (сотрудник, должность, время, филиал)
4. Проверка конфликтов (Warning Box если нужно)
5. Валидация данных
6. Тап "Сохранить"
7. SnackBar с подтверждением
8. Закрытие диалога с `true`
9. Обновление календаря в Schedule Screen

***

## 🚀 ЭТАП 4: Полировка и Финиш
**Цель:** Сделать приложение удобным и красивым.

### ✅ День 12: Уведомления и Обратная связь
*   [ ] **Feedback:** Добавить `SnackBar` уведомления при успешном создании/удалении.
*   [ ] **Error Handling:** Глобальная обработка ошибок (если Mock упал -> показать диалог).

### ✅ День 13: UI Polish (Theming)
*   [ ] **Theme:** Вынести цвета в `AppTheme`.
*   [ ] **Styles:** Унифицировать стили кнопок и инпутов.
*   [ ] **Animations:** Добавить простые анимации переходов (PageTransitions).

### ✅ День 14: Тестирование и Сборка
*   [ ] **Manual Test:** Пройти весь путь пользователя: Вход -> Создание сотрудника -> Назначение смены -> Просмотр профиля.
*   [ ] **Fixes:** Исправление найденных багов верстки.
*   [ ] **Build:** Сборка Web-версии (`flutter build web`).

***

## 🛠 Инструкция: Как работать с этим планом

1.  **Копируй промпты:** Для каждого Дня, где есть UI (День 3, 5, 7, 9, 10), у тебя уже есть готовый, детальный промпт в истории чата.
2.  **Следуй порядку:** Не прыгай сразу на Календарь, не сделав Сотрудников. Календарю нужны данные сотрудников (Resources), которые ты реализуешь на Этапе 2.
3.  **Mock First:** Сначала убедись, что данные приходят в консоль (`print`), потом рисуй UI.

***

## 📚 Созданная документация (2025-11-28)

### Основная документация:
1. **README.md** - Полное руководство разработчика (310 строк)
2. **docs/api_contract.md** - API контракт для backend разработчика (682 строки)
3. **docs/pre_development_analysis.md** - Детальный анализ проблем и решений (682 строки)
4. **docs/critical_fixes_summary.md** - Сводка критических исправлений (200 строк)

### Специализированные гайды:
5. **docs/state_management_guide.md** - State Management стратегия (600 строк)
6. **docs/memory_leaks_prevention.md** - Предотвращение утечек памяти (400 строк)
7. **docs/error_handling_strategy.md** - Единая стратегия обработки ошибок (600 строк)
8. **docs/mock_data_persistence.md** - Стратегия персистентности данных (400 строк)
9. **docs/uuid_generation_guide.md** - Генерация уникальных ID (600 строк)

### Гайды по реализации таблиц:
10. **docs/employee_list_implementation_guide.md** - PlutoGrid реализация (600 строк)
11. **docs/employee_list_syncfusion_guide.md** - Syncfusion DataGrid реализация (700 строк)
12. **docs/table_comparison_pluto_vs_syncfusion.md** - Детальное сравнение решений (180 строк)

### Гайды по реализации календаря:
13. **docs/schedule_screen_implementation_guide.md** - Syncfusion Calendar Timeline реализация (700 строк)

### Гайды по реализации авторизации:
14. **docs/login_screen_implementation_guide.md** - Login Screen с Mock Auth реализация (550 строк)

### Гайды по реализации профиля:
15. **docs/profile_screen_implementation_guide.md** - Profile Screen с TimelineTile + PercentIndicator (650 строк)

### Гайды по реализации диалогов:
16. **docs/create_shift_dialog_implementation_guide.md** - Create Shift Dialog с валидацией и конфликтами (600 строк)

### Архитектурные правила:
17. **.cursor/rules/architecture.mdc** - Обновленные правила с диаграммой зависимостей
18. **.cursor/rules/conventions.mdc** - Конвенции кодирования
19. **.cursor/rules/plan.mdc** - Этот файл (план разработки)

***

## 🎯 Готовность проекта: 100%

### ✅ Что готово:
- Data Layer (модели, repositories, mock API)
- UI компоненты (5 базовых виджетов)
- Routing с Route Guards
- AuthService с session management
- AsyncValue для error handling
- State Management стратегия
- Memory Management правила
- Error Handling стратегия
- UUID Generation паттерны
- Полная документация (19 файлов, 9,274 строки)
- Два готовых плана реализации таблиц (PlutoGrid + Syncfusion)
- Полный план реализации календаря смен (Syncfusion Calendar)
- Полный план реализации Login Screen (Mock Auth)
- Полный план реализации Profile Screen (TimelineTile + PercentIndicator)
- Полный план реализации Create Shift Dialog (с конфликтами и валидацией)
- Детальное сравнение решений

### 🔄 Следующий шаг:
**День 3: Authentication (Login Screen)**

**Тестовые данные:**
```
Email: admin@example.com
Password: password123
```

**Используемые паттерны:**
- `ValueNotifier<AsyncValue<void>>` для состояния логина (Simple Screen)
- `ChangeNotifier` для Medium/High Complexity экранов
- `AuthRepository` для бизнес-логики
- `AuthService` для session management
- `ValueListenableBuilder` / `AnimatedBuilder` для UI updates
- `AsyncValue<T>` для обработки async операций
- **НЕ используется Provider** - прямая инициализация ViewModels